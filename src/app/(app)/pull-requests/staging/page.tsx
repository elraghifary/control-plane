import { EmptyState } from "@/components/states/empty-state";
export default function Page() {
  return <EmptyState title="No pull requests targeting staging" description="Staging syncs land here in a later phase." />;
}
